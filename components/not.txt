  {cardInfo.map((e) => {
              return (
                <CardTypeTow
                  key={e}
                  name={e.name}
                  artist={e.artist}
                  likes={e.likes}
                  price={e.price}
                  image={e.imageNFT}
                />
              );
            })}